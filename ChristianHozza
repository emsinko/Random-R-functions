# Real probability
1/factorial(4)


# Simulated probability_

success = 0
n <- 10^5

for(i in 1:n){
  draw <- sample(letters, 4, replace = FALSE)
  if(all(draw == sort(draw))) success = success + 1
}

success / n
